from setuptools import setup, find_packages  
setup(name = 'ProcessDB', packages = find_packages())
setup(name = 'Algorithm', packages = find_packages())
setup(name = 'DatasetOriginal', packages = find_packages())